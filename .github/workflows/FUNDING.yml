name: Funding Check

on:
  workflow_dispatch:

jobs:
  funding:
    runs-on: ubuntu-latest
    steps:
      - name: Validate funding metadata
        run: echo "Funding config is defined in .github/FUNDING.yml"
